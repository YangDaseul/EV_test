<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- 펼치기 전 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:background="@drawable/bg_ffffff_stroke_e2e2e2" >

        <!-- 접힘/펼침 상태 공통 부분.
             펼친 상태에서 이 부분 배경에 테두리 잘린 것 때문에 울면서 이렇게 한 번 더 포장 -->
        <include
            android:id="@+id/l_service_drive_history_closed_item"
            layout="@layout/layout_service_drive_history_finished"
            app:icon="@{@drawable/btn_arrow_open}" />

    </LinearLayout>
</layout>